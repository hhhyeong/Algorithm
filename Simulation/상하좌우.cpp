#include<iostream>

using namespace std;

int main() {
    int N;
    int x, y, nx, ny;
    char arr[100][100];
    char plan[100];

    char move_types[4] = ['L', 'R', 'U', 'D'];

    cin >> N;
    cin >> plan;

    for (int i = 0; i< N; i++) {
        for (int j = 0; j < N; j++) {
            if (plan[j] == )
        }
    }

}